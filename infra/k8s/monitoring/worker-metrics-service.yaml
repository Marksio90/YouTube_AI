apiVersion: v1
kind: Service
metadata:
  name: worker-metrics
  namespace: ai-media-os
  labels:
    app.kubernetes.io/name: worker-metrics
    app.kubernetes.io/part-of: ai-media-os
    app.kubernetes.io/component: monitoring
spec:
  selector:
    app.kubernetes.io/name: worker
    app.kubernetes.io/component: worker
  ports:
    - name: metrics
      port: 9108
      targetPort: metrics
